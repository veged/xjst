template(this.type === 'simple' && this.mod === 'b') {
  return apply(this.mod = 'b-2');
}

template(this.type === 'simple' && this.mod === 'b-2') {
  return apply(this.end = 'a', this.really = 'b');
}

template(this.type === 'simple' && this.end === 'a' && this.really === 'b') {
  return 'b';
}
