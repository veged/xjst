/* latest template has greatest priority */

template(this.x === 1) { return 1; }
template(this.y === 2) { return 2; }
template(this.y === 2 && this.x === 3) { return 2; }
template(this.x === 3) { return 3; }
