template(this.type === 'simple' && this.mod === 'a') {
  return apply(this.mod = 'a-2');
}

template(this.type === 'simple' && this.mod === 'a-2') {
  return apply(this.end = 'a', this.really = 'a');
}

template(this.type === 'simple' && this.end === 'a' && this.really === 'a') {
  return 'a';
}
